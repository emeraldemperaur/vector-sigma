describe('VΣ Named Export Components Test', () => {
  
  test('Avatar', () => {
    const result = 10;
    expect(result).toBe(10);
  });

  test('Button', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('Checkbox', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Conditional', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('DatePicker', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('DateRangePicker', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('DateTimePicker', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Dropdown', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('File', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('FileMultiple', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Dropdown', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Icons', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Image', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Radio', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Select', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('MultipleSelect', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('Toggle', () => {
     const result = 10;
     expect(result).toEqual(result);
  });

  test('xTitle', () => {
     const result = 10;
     expect(result).toEqual(result);
  });
 
});