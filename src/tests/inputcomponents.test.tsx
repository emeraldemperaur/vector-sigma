describe('VΣ Named Export Input Components Test', () => {
  
  test('Input', () => {
    const result = 10;
    expect(result).toBe(10);
  });

  test('PasswordInput', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('PhoneInput', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('UUIDInput', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('CreditCardInput', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('CurrencyInput', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('StockInput', () => {
    const result = 10;
    expect(result).toEqual(result);
  });

  test('xSlider', () => {
    const result = 10;
    expect(result).toEqual(result);
  });
  
  test('RangeSlider', () => {
    const result = 10;
    expect(result).toEqual(result);
  });
 
});